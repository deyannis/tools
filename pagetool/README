Page and Page Mask calculation tool.
Takes as input a string representing the address in hex format.
Returns the uint value of the address the page it belongs to 
and the page mask.

It is case and "0x" prefix insensitive.

Example of use:
	$ ./pagetool -a 0xFF8C364A
	  Address:	0xFF8C364A
	  Uint Value:	4287379018
	  Page Mask:	0x64A
	  Page:		0xFF8C3000
